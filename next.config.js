// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Tambahkan konfigurasi khusus Next.js di sini
  // Contoh: untuk mengizinkan gambar dari domain tertentu jika Anda menggunakan URL eksternal
  // images: {
  //   domains: ['example.com'],
  // },
};

module.exports = nextConfig;
